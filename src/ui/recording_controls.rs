use gtk::prelude::*;
use std::sync::Arc;
use crate::settings::Settings;

pub fn add_recording_controls_section(content: &gtk::Box, settings: Arc<Settings>) {
    // Move recording controls UI and logic here from main.rs
    // ...
} 